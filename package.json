{
  "name": "waane",
  "version": "1.0.0",
  "description": "Digital Audio Workstation combining a tracker and a node editor",
  "main": "src/index.js",
  "author": "Volcomix",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "live-server --entry-file=/ --no-browser src",
    "jest": "npx --node-arg=--experimental-vm-modules jest",
    "test": "yarn jest --watch",
    "test:ci": "yarn jest",
    "typecheck": "npx -p typescript tsc -p jsconfig.json"
  },
  "devDependencies": {
    "@jest/globals": "^26.6.2",
    "jest": "^26.6.3",
    "live-server": "^1.2.1"
  }
}
