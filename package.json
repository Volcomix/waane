{
  "name": "waane",
  "version": "1.0.0",
  "description": "Digital Audio Workstation combining a tracker and a node editor",
  "main": "src/index.js",
  "author": "Volcomix",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "live-server --entry-file=/ --no-browser src",
    "jest": "NODE_OPTIONS=--experimental-vm-modules npx jest --env=jsdom",
    "test": "yarn jest --watch",
    "test:ci": "yarn jest",
    "typecheck": "npx -p typescript tsc -p jsconfig.json"
  },
  "devDependencies": {
    "@jest/globals": "^27.4.2",
    "jest": "^27.4.6",
    "live-server": "^1.2.1"
  }
}
